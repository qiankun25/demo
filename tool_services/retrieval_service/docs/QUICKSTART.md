## Quickstart (dev)

This stack includes a minimal `indexing_service` so `/semantic_search` works.

```bash
make dev
make health
```

OpenAPI:
- `http://localhost:8003/docs`
- `http://localhost:8003/openapi.json`


